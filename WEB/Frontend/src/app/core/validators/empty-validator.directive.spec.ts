import { EmptyValidatorDirective } from './empty-validator.directive';

describe('EmptyValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptyValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
